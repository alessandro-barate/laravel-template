import './bootstrap';

// Default SCSS file
import '~resources/scss/app.scss';

// Path for images management with Vite
import.meta.glob(["../img/**"]);

// Import all of JS's bootstrap
import * as bootstrap from "bootstrap";